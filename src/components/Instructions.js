import "./Instructions.css";

const Instructions = () => {
    return (
        <div className="instructions">
            <p>Please enter a minimum & maximum number. Click "generate" to generate a random number.</p>
        </div>
    );
};

export default Instructions;